

import { mount } from '@vue/test-utils'
import PostCards from '~/components/post-card.vue'

describe('NuxtLogo', () => {
  it('is a Vue instance', () => {
    
    expect(PostCards).toHaveProperty("")
  })
})
